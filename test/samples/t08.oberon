module TO8 
    var res, undefined_value : int;

    def sum(x, y : int) 
        var a, b : int;
    begin
        res := x + y;
        if (res > 5) then
            a := res + x;
        end
    end
begin 
    sum(3, 4); 
    print(res);
end T01.